/**
 * [코딩 원칙] 
 * 1. 이 파일은 시스템의 모든 "물리적 규격"을 정의하는 최상위 유전자입니다.
 * 2. 모든 수치는 pixel(px) 단위를 기본으로 합니다.
 */

export const SPECS = {
  // 1. 사이드바 및 메뉴 규격
  SIDEBAR_WIDTH: 72,       // 좌측 사이드바 전체 너비
  TILE_SIZE: 64,           // 사이드바 메인 버튼 및 필터 버튼 크기 (64x64)
  TILE_GAP: 8,             // 버튼과 버튼 사이의 수직/수평 간격
  
  // 2. 레이아웃 주요 규격
  HEADER_HEIGHT: 50,       // 상단 바 높이
  DRAWER_WIDTH: 440,       // 우측 상세창/채팅창 슬라이딩 너비
  
  // 3. 디테일 규격
  BORDER_WIDTH: 1,         // 표준 테두리 두께
  RADIUS: 0,               // 사용자님의 디자인 원칙: 모든 모서리는 직각(0px)
  
  // 4. 물리 피드백 수치
  PRESS_DEPTH: 2,          // 클릭 시 버튼이 아래로 내려가는 깊이 (translateY)
};
